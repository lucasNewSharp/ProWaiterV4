name: prowaiter_v2
description: A new Flutter project.

# The following line prevents the package from being accidentally published to
# pub.dev using `pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
version: 2.1+12

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  path_provider: ^2.0.2
  json_annotation: ^4.0.1
  http: ^0.13.3
  package_info: ^2.0.2
  provider: ^6.0.0
  back_button_interceptor: ^5.0.1
  fluttertoast: ^8.0.8
  flutter_launcher_icons: ^0.9.2
  open_file: ^3.0.1
  flutter_downloader: ^1.4.4
  minimize_app: ^0.1.1
  toast: ^0.1.5

  #path_provider: para obter diretorios internos para configuração
  #json_annotation: para ter o gerador de codigo de confersão de objeto para json
  #http: para ter os requests
  #package_info: para obter informações do app
  #provider: para ter o "service provider"
  #back_button_interceptor: biblioteca com facilidades para interceptar o back button
  #fluttertoast: para ter o toast
  #flutter_launcher_icons: ferramenta para adicionar icones do app no Android e IOs
    #comando para gerar os ícones: flutter pub run flutter_launcher_icons:main
  #flutter_downloader: Download do APK e lança a atualização, para andorid versão 5, 5.1 e 6
  #open_file: Para lancar a atualização para android 7+
  #minimize_app: minimiza o app avaliando a plataforma
  #toast: toast


  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.3

flutter_icons:
  android: true
  ios: true
  image_path: "assets/ProWaiter.png"

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.1.1
  json_serializable: ^4.1.4

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/ProWaiter.png    
    - assets/background_login.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/assets-and-images/#resolution-aware.

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/assets-and-images/#from-packages

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/custom-fonts/#from-packages
